import React from 'react';
import { Text, StyleSheet } from 'react-native';

const LoginScreen = () => {
  return <Text style={styles.title}>LoginScreen</Text>;
};

const styles = StyleSheet.create({
  title: {
    fontSize: 32,
  },
});

export default LoginScreen;
